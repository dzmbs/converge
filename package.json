{
  "name": "converge",
  "private": true,
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "contracts:test": "pnpm --dir packages/contracts exec forge test",
    "contracts:build": "pnpm --dir packages/contracts exec forge build",
    "sim:base": "pnpm --dir packages/sim run run:base",
    "sim:stress": "pnpm --dir packages/sim run run:stress"
  }
}
